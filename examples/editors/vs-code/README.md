This is example of using `mrs-build` in VS Code.
Add the `tasks.json` file to `.vscode` folder in your workspace and you are set up.

To run the tasks, invoke the `Tasks: Run Build Task` command (default Ctrl+Shift+B).
